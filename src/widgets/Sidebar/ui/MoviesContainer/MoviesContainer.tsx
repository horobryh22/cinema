import { PopularMovies } from '../MoviesContainer/PopularMovies/PopularMovies';

import { ReturnComponentType } from 'shared/types';

export const MoviesContainer = (): ReturnComponentType => {
    return (
        <>
            <PopularMovies />
        </>
    );
};
